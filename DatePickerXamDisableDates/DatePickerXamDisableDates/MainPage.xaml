<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:DatePickerXamDisableDates"
             x:Class="DatePickerXamDisableDates.MainPage">

    <StackLayout Padding="1" VerticalOptions="Center">
        <!-- Place new controls here -->
        <Label Text="without custom render" HorizontalOptions="Center"></Label>
        <DatePicker MinimumDate="{Binding minimumdate}" MaximumDate="{Binding maximumdate}" x:Name="Mypicker" 
           HorizontalOptions="Center"
           VerticalOptions="Center" 
                   />
        <Label Text="with custom render" HorizontalOptions="Center"></Label>
        <local:DatePickerRender x:Name="DateEvent" 
         PlaceHolder="All"
         HorizontalOptions="Center"
          VerticalOptions="Center">
        </local:DatePickerRender>
    </StackLayout>

</ContentPage>
